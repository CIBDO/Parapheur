/**
 * Charge la police locale JetBrains Mono (fichiers dans /public).
 * Plus de dépendance à Google Fonts / Public Sans.
 */
export async function loadFonts() {
  // La police est déclarée via @font-face dans resources/styles/styles.scss
}

export default function () {
  loadFonts();
}
